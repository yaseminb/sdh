### Schunk Dextrous hand - plotting the full model with tactile data

The existing MATLAB implementation is outdated and requires refactoring and conversion to Python.

The code can be run as-is in MATLAB by executing main.m, which includes example tactile readings visualized in red, as shown in the image.